<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 边框颜色 -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" />

    <!-- 圆角 -->
    <corners
        android:radius="4dp" />

    <!-- 内部填充色（与背景色一致，避免显示区块） -->
    <solid
        android:color="@color/editorBgColor" />
</shape>
